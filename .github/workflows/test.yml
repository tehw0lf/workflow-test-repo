name: Test Workflow

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    permissions:
      contents: read
      actions: write
    uses: tehw0lf/workflows/.github/workflows/build-test-publish.yml@main
    with:
      event_name: ${{ github.event_name }}
      tool: npm
      artifact_path: dist
